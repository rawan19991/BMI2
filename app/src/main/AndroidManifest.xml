<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bmi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BMI">
        <activity android:name=".Edit_Food_Details"></activity>
        <activity android:name=".Food_list" />
        <activity android:name=".add_food_details" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".new_record" />
        <activity android:name=".MoreInformation" />
        <activity android:name=".SignUpScreen" />
        <activity android:name=".LoginScreen" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>